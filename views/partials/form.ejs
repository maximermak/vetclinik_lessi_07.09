<%
  // prefix робить id унікальними: форма існує двічі — у секції та в модальному вікні
  var p = typeof prefix !== 'undefined' ? prefix : 'f';
%>
<form class="form" data-lead-form novalidate>
  <div class="field field--full">
    <span class="field__label">Хто ваш улюбленець?</span>
    <div class="segmented" data-segmented role="radiogroup" aria-label="Вид тварини">
      <span class="segmented__thumb" aria-hidden="true"></span>
      <label class="segmented__opt">
        <input type="radio" name="petType" value="Кіт" checked>
        <svg class="icon" aria-hidden="true"><use href="#i-cat"/></svg>
        <span>Кіт</span>
      </label>
      <label class="segmented__opt">
        <input type="radio" name="petType" value="Собака">
        <svg class="icon" aria-hidden="true"><use href="#i-dog"/></svg>
        <span>Собака</span>
      </label>
      <label class="segmented__opt">
        <input type="radio" name="petType" value="Інша тварина">
        <svg class="icon icon--fill" aria-hidden="true"><use href="#i-paw"/></svg>
        <span>Інша</span>
      </label>
    </div>
  </div>

  <div class="field">
    <label for="<%= p %>-name">Ваше ім’я <span>*</span></label>
    <div class="control">
      <svg class="control__ic icon icon--sm" aria-hidden="true"><use href="#i-user"/></svg>
      <input id="<%= p %>-name" name="name" type="text" autocomplete="name" placeholder="Олена" required>
    </div>
    <small class="err" data-err="name"></small>
  </div>

  <div class="field">
    <label for="<%= p %>-phone">Телефон <span>*</span></label>
    <div class="control">
      <svg class="control__ic icon icon--sm" aria-hidden="true"><use href="#i-phone"/></svg>
      <input id="<%= p %>-phone" name="phone" type="tel" autocomplete="tel" inputmode="tel"
             placeholder="+38 (0__) ___-__-__" required>
    </div>
    <small class="err" data-err="phone"></small>
  </div>

  <div class="field">
    <span class="field__label">Вік тварини</span>
    <div class="agebox">
      <div class="stepper" data-stepper>
        <button class="stepper__btn" type="button" data-step="-1" aria-label="Зменшити вік">
          <svg class="icon icon--sm" aria-hidden="true"><use href="#i-minus"/></svg>
        </button>
        <input class="stepper__val" id="<%= p %>-age" name="petAge" type="text"
               inputmode="numeric" maxlength="2" placeholder="—" aria-label="Вік тварини">
        <button class="stepper__btn" type="button" data-step="1" aria-label="Збільшити вік">
          <svg class="icon icon--sm" aria-hidden="true"><use href="#i-plus"/></svg>
        </button>
      </div>
      <div class="units" data-units role="radiogroup" aria-label="Одиниці віку">
        <label class="units__opt">
          <input type="radio" name="petAgeUnit" value="місяців">
          <span>міс.</span>
        </label>
        <label class="units__opt">
          <input type="radio" name="petAgeUnit" value="років" checked>
          <span>років</span>
        </label>
      </div>
    </div>
  </div>

  <div class="field">
    <span class="field__label">Послуга</span>
    <div class="dropdown" data-dropdown>
      <input type="hidden" name="service" value="">
      <button class="dropdown__btn" type="button" aria-haspopup="listbox" aria-expanded="false">
        <span class="dropdown__value is-placeholder">Оберіть послугу</span>
        <svg class="icon icon--sm dropdown__chev" aria-hidden="true"><use href="#i-chev"/></svg>
      </button>
      <ul class="dropdown__list" role="listbox" tabindex="-1" aria-label="Послуги">
        <% serviceOptions.forEach(function (o, i) { %>
          <li class="dropdown__opt" role="option" aria-selected="false"
              data-value="<%= o %>" style="--i: <%= i %>"><%= o %></li>
        <% }) %>
      </ul>
    </div>
  </div>

  <div class="field field--full">
    <label for="<%= p %>-time">Бажаний час візиту</label>
    <div class="control">
      <svg class="control__ic icon icon--sm" aria-hidden="true"><use href="#i-clock"/></svg>
      <input id="<%= p %>-time" name="preferredTime" type="text" placeholder="Завтра після 15:00">
    </div>
  </div>

  <div class="field field--full">
    <label for="<%= p %>-msg">Що турбує?</label>
    <textarea id="<%= p %>-msg" name="message" rows="3"
              placeholder="Коротко опишіть симптоми або питання"></textarea>
  </div>

  <input class="hp" type="text" name="website" tabindex="-1" autocomplete="off" aria-hidden="true">

  <button class="btn btn--primary btn--wide" type="submit">
    <svg class="icon icon--fill" aria-hidden="true"><use href="#i-tg"/></svg>
    <span class="btn__label">Надіслати заявку</span>
  </button>

  <p class="form__note">Натискаючи кнопку, ви погоджуєтесь на обробку персональних даних.</p>
  <p class="form__status" role="status" aria-live="polite"></p>
</form>

<div class="form__done" hidden>
  <span class="form__done-ic"><svg class="icon" aria-hidden="true"><use href="#i-check"/></svg></span>
  <h3>Дякуємо, заявку прийнято!</h3>
  <p>Адміністратор передзвонить вам найближчим часом у робочі години.</p>
  <button class="btn btn--ghost btn--sm" type="button" data-reset>Надіслати ще одну</button>
</div>
